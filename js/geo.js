window.ZONQR_GEO=(function(){const cache=new Map();function key(lat,lng){return lat.toFixed(4)+','+lng.toFixed(4)}async function reverse(lat,lng){const k=key(lat,lng);if(cache.has(k))return cache.get(k);try{const r=await fetch('https://api.bigdatacloud.net/data/reverse-geocode-client?latitude='+lat+'&longitude='+lng+'&localityLanguage=es');if(r.ok){const d=await r.json();const out={label:d.locality||d.city||'Tu zona',detail:[d.locality,d.principalSubdivision].filter(Boolean).join(', '),full:[d.locality||d.city,d.principalSubdivision,d.countryName].filter(Boolean).join(', ')};cache.set(k,out);return out}}catch(e){}try{const r=await fetch('https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat='+lat+'&lon='+lng+'&accept-language=es');if(r.ok){const d=await r.json();const a=d.address||{};const street=[a.road,a.house_number].filter(Boolean).join(' ');const loc=a.neighbourhood||a.suburb||a.city||a.town||'Tu zona';const out={label:street||loc,detail:[street,loc,a.state].filter(Boolean).join(', '),full:d.display_name||loc};cache.set(k,out);return out}}catch(e){}return null}return{reverse}})();
